//changing all upper case to lower case in a given text file

#include"header.h"

int main()
{
	char *str;
	FILE *fp;

	fp = fopen("sample.txt", "r+");//opening the file
	
	str = (char *)malloc(size * sizeof(char));//allocating memory to string str

	fwrite(

	str_upr(str);

	printf("%s\n",str);

	return 0;
}
